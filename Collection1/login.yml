info:
  name: login
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5000/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "bilal2@example.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​